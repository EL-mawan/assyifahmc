import type { Metadata } from "next";
import "./globals.css";
import { ToastContainer } from 'react-toastify';
import 'react-toastify/dist/ReactToastify.css';

export const metadata: Metadata = {
  title: "Assyifah MC - Master of Ceremony Profesional",
  description: "Jasa MC profesional untuk pernikahan, acara perusahaan, dan berbagai acara spesial lainnya di Jakarta dan sekitarnya.",
  keywords: "MC, Master of Ceremony, Wedding MC, Corporate Event MC, Professional MC, Jakarta MC",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="id">
      <body>
        {children}
        <ToastContainer 
          position="top-right"
          autoClose={3000}
          hideProgressBar={false}
          newestOnTop
          closeOnClick
          rtl={false}
          pauseOnFocusLoss
          draggable
          pauseOnHover
          theme="light"
        />
      </body>
    </html>
  );
}
